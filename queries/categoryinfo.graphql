query($channels:ChannelFilter!) {
    categoryInfo(channels:$channels) {
        categories {
            name
            id
            values {
                name
                id
            }
        }
    }
}
